{
  "name": "realest-reset",
  "version": "0.1.0",
  "private": true,
  "description": "Black/gold service site with Stripe Checkout, quote intake, and quote admin for Vercel.",
  "scripts": {
    "dev": "vercel dev",
    "check": "node --check script.js && node --check quote.js && node --check admin.js && node --check api/quotes.js"
  },
  "dependencies": {},
  "devDependencies": {
    "vercel": "^50.28.0"
  }
}
