{
  "name": "realest-reset",
  "version": "0.1.0",
  "private": true,
  "description": "Minimal dark one-page service site with Stripe Checkout via Netlify Functions.",
  "scripts": {
    "dev": "netlify dev",
    "check": "node --check script.js && node --check netlify/functions/create-checkout-session.js"
  },
  "dependencies": {
    "stripe": "^18.5.0"
  },
  "devDependencies": {
    "netlify-cli": "^24.11.3"
  }
}
