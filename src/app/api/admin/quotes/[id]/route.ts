import { NextResponse } from "next/server"; import { z } from "zod"; import { isAdmin } from "@/lib/auth"; import { ensureSchema,getDb } from "@/lib/db";
const status=z.enum(["new","contacted","scheduled","completed","archived"]);
export async function GET(_:Request,{params}:{params:Promise<{id:string}>}){if(!await isAdmin())return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;await ensureSchema();const [quote]=await getDb()`SELECT * FROM quote_requests WHERE id=${id}`;return quote?NextResponse.json({quote}):NextResponse.json({error:"Not found"},{status:404})}
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){if(!await isAdmin())return NextResponse.json({error:"Unauthorized"},{status:401});const {id}=await params;const parsed=status.safeParse((await req.json()).status);if(!parsed.success)return NextResponse.json({error:"Invalid status"},{status:400});await ensureSchema();const [quote]=await getDb()`UPDATE quote_requests SET status=${parsed.data} WHERE id=${id} RETURNING *`;return quote?NextResponse.json({quote}):NextResponse.json({error:"Not found"},{status:404})}
